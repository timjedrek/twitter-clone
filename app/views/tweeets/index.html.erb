<p style="color: green"><%= notice %></p>

<section class="section">
  <div class="container">
    <div class="columns">
      <%= render 'feed' %>
      <%= render 'who-to-follow' %>
    </div>
  </div>
</section>
